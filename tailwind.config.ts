// Tailwind CSS v4 uses @theme in CSS files instead of this config
// This file is kept for compatibility and darkMode setting
import type { Config } from "tailwindcss";

const config: Config = {
  darkMode: "class",
};

export default config;

